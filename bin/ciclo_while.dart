void main() {
  int i = 1; // Inicializamos la variable

  // Ciclo while para imprimir números del 1 al 10
  while (i <= 10) {
    print('Número: $i');
    i++; // Incrementamos la variable en cada iteración
  }
}
